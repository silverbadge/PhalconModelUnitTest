language: php

before_script:
  - chmod a+x ./tests/install-php-extension.sh
  - ./tests/install-php-extension.sh
  - composer self-update
  - composer update --dev -o

php:
  - 5.4
  - 5.5

script:
  - phpunit --configuration ./phpunit.xml

